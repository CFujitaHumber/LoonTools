# Workflow

It's best to keep verbose mode on

Install updates only to LoonTools, LoonE, or Config
```
./LoonEnv -v -s
```

Install only docker changes with script updates
```
./LoonEnv -v -f docker -s
```

Install updates to everything, including MTU configuration
```
./LoonEnv -v
```
